{
  "name": "sas-hot-editor",
  "version": "0.1.1",
  "description": "Table editor for SAS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install",
    "server": "http-server app/ -a localhost -p 8000 -c-1",
    "build": "gulp build"
  },
  "author": "Boemskats",
  "license": "GPL v3",
  "devDependencies": {
    "http-server": "^0.8.0",
    "gulp": "^3.9.1",
    "gulp-if": "^2.0.1",
    "gulp-minify-css": "^1.2.4",
    "gulp-uglify": "^1.5.4",
    "gulp-useref": "^3.1.0"
  },
  "dependencies": {}
}
